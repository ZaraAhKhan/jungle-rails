<h1> Dashboard </h1>
<hr />
<p>There are <%= @product_count%> <%= link_to 'Products',  [:admin, :products] %> in the store </p>

<br />
<div class="card" >
  <div class="card-header">
<p>There are <%= @category_count%> categories in the store</p>
</div>
<ul class="list-group list-group-flush">
<% @categories_all.each do |category| %>
              <li class="list-group-item"><%= link_to category.name, category %></li>
            <% end %>